### Why reduce() is useful here

`reduce()` helps transform an array into:

- a single value → total expense
- an object → category grouping
- a computed result → max expense

This is why it’s heavily used in:

- backend APIs
- analytics systems
- transaction processing
- dashboard aggregations
