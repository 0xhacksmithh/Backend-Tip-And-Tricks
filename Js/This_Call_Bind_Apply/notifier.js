export function sendNotification(food, price) {
  console.log(`
        Hello ${this.name}
        Your order of ${food} is Confirmed.
        Total Price: ${price}
        Delivery City: ${this.city}
        `);
}
